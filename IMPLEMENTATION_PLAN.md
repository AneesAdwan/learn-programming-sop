# خطة العمل وتقسيم المهام - منصة تعليم ويب شاملة

## 📋 نظرة عامة
هذا الملف يحتوي على خارطة الطريق وتقسيم المهام لتنفيذ مشروع المنصة التعليمية الشاملة لبرمجة الويب، مع الالتزام بمبدأ "الشمولية الكاملة" والعناية الفائقة بالتصميم وتجربة المستخدم.

---

## 📅 مراحل التنفيذ (Phases)

### 🔹 المرحلة 1: التأسيس والبنية التحتية (Setup & Infrastructure)
- [ ] **إعداد هيكلية المجلدات والملفات**:
    - إنشار المجلدات: `html-page`, `css-page`, `js-page`, `assets`, `scripts`, `styles`.
    - إنشاء الملفات الرئيسية فارغة.
- [ ] **بناء ملفات الأنماط الأساسية (Base Styles)**:
    - `styles/variables.css`: تعريف متغيرات الألوان (HTML Orange, CSS Blue, etc)، الخطوط، والتباعد.
    - `styles/reset.css`: تصفير إعدادات المتصفح.
    - `styles/main.css`: التنسيقات العامة والوضع الليلي (Dark Mode variables).
- [ ] **تحضير الأصول (Assets)**:
    - تنزيل أو تجهيز أيقونات اللغات (SVGs).
    - تجهيز الخطوط العربية (Cairo/Tajawal).

### 🔹 المرحلة 2: الواجهة الرئيسية والهيكل العام (Core UI & Landing Page)
- [ ] **تطوير الصفحة الرئيسية (index.html)**:
    - تصميم **Hero Section** ترحيبي جذاب.
    - تصميم **بطاقات اللغات الثلاث** (HTML, CSS, JS) مع تأثيرات Hover متقدمة.
- [ ] **مكونات التخطيط المشتركة (Layout Components)**:
    - تصميم **Navbar** (شريط التنقل) الثابت والمتجاوب.
    - تصميم **Footer** (التذييل) يحتوي على الحقوق: **"برمجة وتطوير أنيس عدوان"**.
- [ ] **الوضع الليلي (Dark Mode)**:
    - تنفيذ زر التبديل (Toggle Switch).
    - تطبيق متغيرات الألوان على العناصر الرئيسية.

### 🔹 المرحلة 3: محرك النظام (The Core Engine)
هذه المرحلة هي القلب النابض للمنصة، حيث سنبني المنطق الذي يولد المحتوى ديناميكياً.
- [ ] **تصميم هيكل البيانات (Data Structure)**:
    - تعريف شكل كائن JSON الموحد للبطاقة (Syntax, Values, Examples).
- [ ] **تطوير `scripts/card-generator.js`**:
    - دالة لاستقبال البيانات وإنشاء HTML Nodes للبطاقات.
    - معالجة أنواع المحتوى المختلفة (نص، كود، جدول).
- [ ] **تطوير `scripts/main.js`**:
    - إدارة التنقل (Routing) البسيط (إظهار/إخفاء الأقسام).
    - إدارة القوائم المنسدلة (Sidebar/Dropdowns) بناءً على التصنيفات في البيانات.
- [ ] **تطوير `scripts/copy-code.js`**:
    - وظيفة نسخ الكود للحافظة مع رسالة تأكيد (Toast).

### 🔹 المرحلة 4: وحدة HTML (التنفيذ والمحتوى)
- [ ] **إنشاء صفحة `html-page/html.html`**.
- [ ] **بناء ملف البيانات `html-page/html-data.js`**:
    - إدخال بيانات العناصر (Tags) وتصنيفها.
    - التأكد من تغطية 100% من العناصر (الرئيسية، الدلالية، النماذج، إلخ).
- [ ] **تخصيص التصميم**: تطبيق الثيم البرتقالي الخاص بـ HTML.

### 🔹 المرحلة 5: وحدة CSS (التنفيذ والمحتوى)
- [ ] **إنشاء صفحة `css-page/css.html`**.
- [ ] **بناء ملف البيانات `css-page/css-data.js`**:
    - إدخال خصائص CSS وتصنيفها (Box Model, Flexbox, Grid, Typography...).
    - إضافة أمثلة مرئية (Visual Examples) داخل البطاقات.
- [ ] **تخصيص التصميم**: تطبيق الثيم الأزرق الخاص بـ CSS.

### 🔹 المرحلة 6: وحدة JavaScript (التنفيذ والمحتوى)
- [ ] **إنشاء صفحة `js-page/js.html`**.
- [ ] **بناء ملف البيانات `js-page/js-data.js`**:
    - المحتوى الأضخم: المتغيرات، الدوال، الـ DOM، الأحداث، ES6+.
- [ ] **تخصيص التصميم**: تطبيق الثيم الأصفر الخاص بـ JS.

### 🔹 المرحلة 7: الاختبار، التحسين، والإطلاق (QA & Polish)
- [ ] **اختبار التجاوب (Responsive Testing)**: موبايل، تابلت، ديسكتوب.
- [ ] **التدقيق اللغوي**: مراجعة النصوص العربية.
- [ ] **تهيئة SEO**: إضافة Meta Tags والعناوين الصحيحة.
- [ ] **الأداء**: التأكد من سرعة التحميل وعدم وجود تعليق (Lag) عند عرض مئات البطاقات.

---

## 🛠 التكنولوجيا المستخدمة (Tech Stack)
- **HTML5**: دلالي (Semantic).
- **CSS3**: Variables, Flexbox, Grid, Transitions (No Frameworks).
- **JavaScript (ES6+)**: Vanilla JS, Modules (إذا لزم الأمر)، DOM Manipulation.
- **الخطوط**: Google Fonts (Cairo / Tajawal).

## ⚠️ ملاحظات هامة
- **التذييل**: يجب أن يظهر في كافة الصفحات نص: "برمجة وتطوير أنيس عدوان".
- **عدم الإسقاط**: أي عنصر برمجي مهما كان صغيراً يجب أن يكون له بطاقة.
- **التصميم**: يجب أن يكون مبهراً (Wow Factor) وليس مجرد موقع تعليمي تقليدي.
